# SHealth BMI — UnitTest 작성 계획 프롬프트

> 이 파일은 Cursor AI에 붙여 넣어 실행하는 **프롬프트**입니다.  
> 산출물: `docs/unitest_plan.md` (README Activity 3 대응 TC 로드맵·체크리스트).

---

## 실습 활동 (README 연계)

본 프롬프트는 `README.md` **「생성형AI를 활용한 Activities」** 중 아래 항목을 수행합니다.

```markdown
3. UnitTest 작성 (1시간)
- [ ] BMI 계산 로직 TC
- [ ] Age 평균치 보정 로직 TC
- [ ] 정상/저체중/과체중/비만 분류 TC
- [ ] 예외상황 TC
```

| README 체크 항목 | 본 프롬프트에서 다루는 내용 |
|------------------|---------------------------|
| BMI 계산 로직 TC | `computeBmi`·cm→m 변환·처리 건수·알려진 weight/height → 기대 BMI |
| Age 평균치 보정 로직 TC | `weight==0` 동일 연령대 `[a,a+10)` 평균 보정·`ageCount==0`·연령대 간 독립 |
| 정상/저체중/과체중/비만 분류 TC | BMI 경계 18.5·23·25, type 100~400, `getBmiRatio` 집계·합≈100% |
| 예외상황 TC | 파일 없음·헤더만·잘못된 ageClass/type·빈 줄·0 나눗셈 방지 |

### 프롬프트 실행 완료 시 README 체크

`docs/unitest_plan.md` 저장이 끝나고, 계획에 명시된 **필수 TC가 `src/test/cpp/SHealthBMITest.cpp`에 구현·Green**이면  
`README.md`의 해당 체크박스를 완료 표시합니다.

- `- [ ]` → `- [x]` 로 변경
- 대상 줄 (Activity 3):
  - `BMI 계산 로직 TC`
  - `Age 평균치 보정 로직 TC`
  - `정상/저체중/과체중/비만 분류 TC`
  - `예외상황 TC`

README 수정만으로 커밋하지 않습니다. 사용자가 요청한 경우에만 커밋합니다.

---

## 프롬프트 본문 (아래부터 복사)

---

**User**

@src/main/cpp/SHealth.h @src/main/cpp/SHealth.cpp @src/test/cpp/SHealthBMITest.cpp @docs/requirements_analysis.md @docs/code_quality_report.md @docs/refactoring_plan.md @tasks/0.setting.md @README.md

[P] 시니어 C++ QA·TDD 코치입니다. Google Test 기반으로 **테스트 우선**으로 도메인을 고정합니다.

[C] 제약  
- **브랜치·맥락**: `tc` 브랜치. `refactoring`에서 분기한 1차 리팩토링 완료 상태를 전제로, **Activity 3(단위 테스트)** 만 범위로 한다.  
- **공개 API 시그니처 유지**: `int calculateBmi(const std::string& filename)`, `double getBmiRatio(int ageClass, int type)` — 반환 의미 변경 금지.  
- **도메인 규칙 고정** (`README.md` / `docs/requirements_analysis.md` / `tasks/0.setting.md`):  
  - BMI = kg / (m)², m = cm/100  
  - 분류: ≤18.5 저체중(100), 18.5<BMI<23 정상(200), 23≤BMI<25 과체중(300), BMI≥25 비만(400)  
  - 연령대 `[a,a+10)`, a∈{20,30,40,50,60,70}  
  - `weight==0` → 동일 연령대 유효 체중 평균 보정  
- **테스트 격리**: `shealth.dat` 전체 의존 금지. `TEST_F` fixture에서 **임시 CSV** 생성·`TearDown`에서 삭제 (현재 `writeCsv` 패턴 유지).  
- **검증**: 각 TC 추가·수정 후 `cmake --build . && ctest --output-on-failure` Green. Red 상태에서 대량 리팩토링 금지.  
- **스펙 vs 레거시**: 불일치 시 **README 스펙 + 테스트**가 진실의 원천. 계획서에 Red/Green 여부를 명시.  
- **범위 외**: README Activity 4(신규 API·height=0 보정·SRP 분리)는 **참고만** — 본 계획 §5.6 시나리오 26~30은 “후속”으로 분리.

[T] README Activity 3 네 항목을 충족하는 **Google Test 작성·보강 계획**을 제안해줘. (`calculateBmi`·`getBmiRatio` 파이프라인 중심.)

1. **현황 갭 분석**  
   - `src/test/cpp/SHealthBMITest.cpp` **기존 13개** `TEST_F`를 표로 매핑 (테스트명 | README 4항목 중 해당 | `docs/requirements_analysis.md` §5 시나리오 번호 | 상태: 구현됨/부분/미구현)  
   - README 4체크박스별 **필수 TC 최소 세트**와 **갭(추가·보강 필요)** 목록

2. **BMI 계산 로직 TC** (README 1번째 체크)  
   - 직접 BMI 검증 vs `getBmiRatio` 간접 검증 전략  
   - 필수: 알려진 weight/height → BMI, cm→m, `calculateBmi` 반환 건수  
   - `EXPECT_NEAR` ε(예: `1e-4`)·Given-When-Then 주석 규칙

3. **Age 평균치 보정 로직 TC** (README 2번째 체크)  
   - 동일 연령대 1건 0 / 전원 0(`ageCount==0`) / 타 연령대 0 / 20·30대 각각 보정  
   - 보정 후 집계 검증 방법 (`getBmiRatio` 합≈100% 또는 명시적 기대 BMI)

4. **정상/저체중/과체중/비만 분류 TC** (README 3번째 체크)  
   - 경계: 18.5, 18.5+ε, 23, 24.99, 25 (**BMI=25 비만** 스펙 고정)  
   - 연령 경계: 19(20대 제외), 20·29, 30(30대 포함)  
   - 4분류 혼합 시 연령대별 type 합 ≈ 100%

5. **예외상황 TC** (README 4번째 체크)  
   - 파일 없음, 헤더만, 잘못된 `ageClass`/`type`, 빈 데이터 줄 정책  
   - 0 나눗셈·크래시 없음 (`ImputeWeight_AllZeroInBand` 등)

6. **TDD 실행 순서·커밋 단위**  
   - Red→Green→Refactor 순서 (한 커밋 = 한 테스트 묶음 또는 한 경계)  
   - 커밋 메시지 예시: `test: add BMI boundary at 25`, `test: impute weight zero in age band`  
   - **tc 브랜치**에 푸시 전 체크리스트

[F] 다음 형식으로 작성하고 **`docs/unitest_plan.md`**에 저장 (없으면 생성, 있으면 전체 덮어쓰기).

### 필수: README 4항목 × TC 매트릭스 (Markdown pipe table)

| README 체크 항목 | 필수 TC (테스트 이름) | Given 요약 | Then (검증) | §5 시나리오 # | 상태 |
|------------------|----------------------|------------|-------------|---------------|------|
| BMI 계산 로직 TC | … | … | … | 1~5 | 구현됨/갭 |
| Age 평균치 보정 … | … | … | … | 6~10 | … |
| 분류 TC | … | … | … | 11~18 | … |
| 예외상황 TC | … | … | … | 23~25 | … |

- 표는 **4개 README 행마다 최소 4개 TC 행**(총 **최소 16행** 이상, 기존·신규 구분)  
- “상태” 열: `✅ 구현`, `⚠️ 부분`, `⬜ 갭`

### 필수: 기존 테스트 인벤토리 표

| # | TEST_F 이름 | README 4분류 | §5 # | 비고 |
|---|-------------|---------------|------|------|
| 1 | CalculateBmi_FileNotFound_ReturnsZero | 예외 | 23 | … |
| … | (13개 전부) | … | … | … |

### 필수: 본문 섹션 (표 외)

1. **제약·전제** — API, CSV, 스펙, fixture 패턴  
2. **Phase A — BMI 계산** — 추가 TC 목록·fixture 예시(1~2줄 CSV)  
3. **Phase B — Age 보정** — 추가 TC·`ageCount==0` 합의안  
4. **Phase C — 4분류·경계** — Red였던 항목(BMI=25) Green 확인  
5. **Phase D — 예외·파일** — 빈 줄·잘못된 입력 정책  
6. **구현 우선순위** — `docs/requirements_analysis.md` §6 TDD 순서와 정렬  
7. **리스크·회귀** — 부동소수점, 연령 구간, 비율 sum=0  
8. **완료 정의 (DoD)** — 4 README 체크박스 ↔ `ctest` 100% pass ↔ 최소 TC 개수  

### 필수: 검증 블록 (복사 가능)

```bash
mkdir -p build && cd build
cmake ..
cmake --build .
ctest --output-on-failure
```

- Windows: `build` 디렉터리에서 동일  
- 실패 시: 실패 테스트명·원인·수정안 1~2문장

한국어. 전체 `SHealthBMITest.cpp` 붙여넣기 금지. 코드 인용은 테스트명·한 줄 Given만.

**완료 후 (TC 구현까지 수행한 경우에만):** `README.md` Activity 3의  
`BMI 계산 로직 TC`, `Age 평균치 보정 로직 TC`, `정상/저체중/과체중/비만 분류 TC`, `예외상황 TC`를 `- [x]`로 체크.

---

## AI 작업 지침 (프롬프트 실행 시 준수)

### 참조 우선순위

1. `docs/requirements_analysis.md` §5 — 시나리오 1~25 (Activity 3 범위)  
2. `src/test/cpp/SHealthBMITest.cpp` — **현재 13개 테스트** (2026-05 기준 Green)  
3. `tasks/0.setting.md` — Given-When-Then, `EXPECT_NEAR`, 경계값 표  
4. `SHealth.h` / `SHealth.cpp` — `classifyBmi`, `imputeMissingWeightsForBand`, `computeBmi`  
5. `docs/refactoring_plan.md` — Phase 0 “테스트 기반 구축”과 중복·정렬

### [T] 섹션별 계획 깊이

#### 1) 현황 — 기존 13개 테스트 ↔ README 매핑 (계획서에 반드시 반영)

| TEST_F (기존) | README 4항목 | §5 # |
|---------------|-------------|------|
| `CalculateBmi_FileNotFound_ReturnsZero` | 예외 | 23 |
| `CalculateBmi_MultipleRows_ReturnsCorrectCount` | BMI 계산 | 5 |
| `ClassifyBmi_At25_IsObesity` | 분류 | 14 |
| `ClassifyBmi_At18_5_IsUnderweight` | 분류 | 11 |
| `ClassifyBmi_At23_IsOverweight` | 분류 | 13 |
| `ClassifyBmi_JustBelow25_IsOverweight` | 분류 | 15 |
| `ImputeWeight_OneZeroInAgeBand_UsesPeerAverage` | Age 보정 | 6 |
| `ImputeWeight_AllZeroInBand_NoDivisionByZero` | Age 보정 | 7 |
| `GetBmiRatio_FourTypes_SumNear100` | 분류 | 20 |
| `GetBmiRatio_InvalidType_ReturnsZero` | 예외 | 21 |
| `GetBmiRatio_InvalidAgeClass_ReturnsZero` | 예외 | 22 |
| `ClassifyBmi_Age19_ExcludedFrom20Band` | 분류 | 17 |
| `CalculateBmi_HeaderOnly_ReturnsZero` | 예외 | 24 |

#### 2) 갭 — README 4체크박스별 **권장 추가 TC** (계획서 “⬜ 갭”에 명시)

| README 항목 | 권장 추가 TEST_F | 목적 |
|-------------|------------------|------|
| BMI 계산 | `CalculateBmi_StandardWeightHeight_ReturnsExpectedBmi` | 70kg/170cm → BMI ≈ 24.22 (`computeBmi` 간접 또는 ratio) |
| BMI 계산 | `CalculateBmi_HeightInMeters_ConvertsFromCm` | 180cm → m 변환 |
| Age 보정 | `ImputeWeight_ZeroOutsideBand_Unchanged` | 30대 0이 20대 평균에 영향 없음 |
| Age 보정 | `ImputeWeight_TwoBands_IndependentAverages` | 20·30대 각 0 1건, 대별 다른 보정 |
| 분류 | `ClassifyBmi_JustAbove18_5_IsNormal` | 18.5+ε → type 200 |
| 분류 | `ClassifyBmi_Between18_5And23_IsNormal` | BMI=21 → 정상 |
| 분류 | `ClassifyBmi_Age30_In30Band` | age 30 → 30대 집계 |
| 예외 | `CalculateBmi_EmptyLine_StopsOrSkips` | 빈 줄 후 count 정책 고정 |

- 위 갭 TC는 **필수 최소**; 계획서에서 우선순위·커밋 단위로 배치할 것.  
- 이미 Green인 13개는 **삭제·약화 금지** — 보강만.

#### 3) TDD 커밋 쪼개기 가이드

| 커밋 | 대상 | 금지 |
|------|------|------|
| T1 | BMI 계산 1~2 TC 추가 | 동시에 `SHealth.cpp` 대규모 리팩토링 |
| T2 | Age 보정 갭 TC | height=0 신규 기능 |
| T3 | 분류 경계 갭 TC | API 시그니처 변경 |
| T4 | 예외·파일 갭 TC | README Activity 4 기능 |
| T5 | `docs/unitest_plan.md` + README `[x]` (사용자 요청 시 커밋) | 테스트 Red인 채 체크 |

#### 4) Fixture·검증 패턴 (계획서에 코드 스니펫 5줄 이내)

```cpp
// Given
writeCsv("1,25,70,170\n");
SHealth shealth;
const int n = shealth.calculateBmi(tempCsvPath_);
// When / Then
EXPECT_EQ(n, 1);
EXPECT_NEAR(shealth.getBmiRatio(20, 200), 100.0, 0.01);
```

- BMI 직접 검증이 어려우면 **공개 API만**으로: `getBmiRatio(ageClass, type)==100` 등 집계 검증을 계획서에 명시.  
- `private computeBmi` 테스트는 **friend 또는 간접 검증** 중 계획서에서 1안 선택.

### 산출물 품질 기준

- README 4체크박스 각각 **최소 3개 이상** TC가 매핑되어야 “완료” 가능.  
- `docs/requirements_analysis.md` §5 시나리오 **1~25** 중 Activity 3 관련 항목의 **커버리지 %** 표기.  
- 시나리오 **26~30**은 “Activity 4 후속” 절로 분리.  
- 기존 13 테스트와 충돌하는 기대값 변경 시 **스펙 근거** 한 줄 필수.

### 저장

| 산출물 | 경로 |
|--------|------|
| UnitTest 작성 계획·체크리스트 | `docs/unitest_plan.md` |

- 부분 패치 금지, **전체 덮어쓰기**

### 완료 후 README 체크 표시

`docs/unitest_plan.md` 저장 후, **실제 TC 구현·`ctest` Green**이 확인되면 `README.md` Activity 3을 갱신.

```diff
- - [ ] BMI 계산 로직 TC
+ - [x] BMI 계산 로직 TC
- - [ ] Age 평균치 보정 로직 TC
+ - [x] Age 평균치 보정 로직 TC
- - [ ] 정상/저체중/과체중/비만 분류 TC
+ - [x] 정상/저체중/과체중/비만 분류 TC
- - [ ] 예외상황 TC
+ - [x] 예외상황 TC
```

- 계획서만 작성하고 TC 미구현이면 **체크하지 않음**.

### 금지·주의

- 본 프롬프트 **1차 실행**은 `docs/unitest_plan.md` **계획만** 산출 가능.  
- 사용자가 “계획대로 TC 구현”을 요청할 때 `SHealthBMITest.cpp` 수정.  
- `shealth.dat` 포맷·공개 API 변경을 전제로 한 TC는 **금지**.  
- 한 커밋에 “계획서 + 8개 TC + 리팩토링” 묶기 **불합격**.

---

## Gilded Rose / 이전 tasks와의 대응

| 이전 task | 산출물 | 본 task (Activity 3) |
|-----------|--------|----------------------|
| `tasks/2.code_quality_prompt.md` | `docs/code_quality_report.md` | 스멜·테스트 부재 근거 인용 |
| `tasks/3.refactoring_plan_prompt.md` | `docs/refactoring_plan.md` | Phase 0 테스트와 정렬, 구현은 tc 브랜치 |
| `tasks/1.prd.md` | `docs/requirements_analysis.md` | §5 시나리오 1~25 = TC 목록 원천 |
| **본 파일** | `docs/unitest_plan.md` | README 73~77행 4체크박스 완료 경로 |

---

## 실행 체크리스트 (프롬프트 실행 후)

- [ ] 기존 13개 `TEST_F` 인벤토리 표가 포함되었는가?  
- [ ] README 4항목 × TC 매트릭스 **16행 이상**인가?  
- [ ] §5 시나리오 1~25와 갭·우선순위가 정렬되었는가?  
- [ ] Phase A~D·TDD 커밋 단위·검증 명령이 있는가?  
- [ ] Activity 4(26~30)가 후속으로 분리되었는가?  
- [ ] `docs/unitest_plan.md` 저장 완료  
- [ ] (선택) TC 구현·`ctest` Green 후 README Activity 3 체크 `[x]` 완료
