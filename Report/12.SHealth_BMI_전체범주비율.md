# SHealth BMI — 전체 사용자 BMI 범주 비율 기능 실행 결과 보고서

| 항목 | 내용 |
|------|------|
| 프로젝트 | SHealth_BMI_07 |
| 작성일 | 2026-05-20 |
| 프롬프트 | `tasks/5.feature_plan.md` (Phase F5), `tasks/ROUTINE.md` (21–33) |
| Phase | F5 — 전체 사용자 대비 BMI 범주 비율 |
| 목적 | README Activity 4 — **전체 사용자 대비 각 BMI 범주 비율 계산 기능 추가** |

---

## 1. 실행 개요

| 항목 | 내용 |
|------|------|
| 범위 | `aggregateOverallRatios`, `getOverallBmiRatio`, §5 #28 TC + 25% 혼합 TC |
| 공개 API | 기존 API 유지 + `getOverallBmiRatio(int type) const` **추가** |
| 검증 | `cmake --build .` + `ctest --output-on-failure` — **29/29 Passed** |
| 신규 TC | `GetOverallBmiRatio_AllObese_Returns100PercentObesity` (#28) |
| | `GetOverallBmiRatio_FourTypes_Each25Percent` (4분류 혼합) |

---

## 2. README Activity 4 — 실행 결과

| 체크 항목 | 상태 |
|-----------|------|
| SRP 책임 분리 | ✅ |
| 연령대 BMI 분포 비율 | ✅ |
| Height=0 평균치 보정 | ✅ |
| BMI 정상 범위 사용자 목록 | ✅ |
| **전체 사용자 BMI 범주 비율** | ✅ |

**Activity 4 기능 개선 5항목 전체 완료.**

---

## 3. API 책임 분리

| 메서드 | 집계 범위 | 저장소 |
|--------|-----------|--------|
| `getBmiRatio(ageClass, type)` | 연령대 `[a,a+10)` | `bmiRatios_[band][category]` |
| `getOverallBmiRatio(type)` | **전체** `recordCount_` | `overallBmiRatios_[category]` |

파이프라인: `… → aggregateRatiosByAgeBand() → aggregateOverallRatios()`

---

## 4. 구현 요약

| 변경 | 내용 |
|------|------|
| `overallBmiRatios_` | 전체 4분류 비율(%) 저장 |
| `aggregateOverallRatios()` | 연령 무관 전 레코드 분류 집계 |
| `getOverallBmiRatio(type)` | type→category 인덱스 lookup, 잘못된 type → 0.0 |
| `calculateBmi` | 집계 단계에 `aggregateOverallRatios()` 추가 |

---

## 5. 신규 테스트

### `GetOverallBmiRatio_AllObese_Returns100PercentObesity` (§5 #28)

| Given | Then |
|-------|------|
| 25·35·45세 각 90kg/170cm (비만) | `getOverallBmiRatio(400)` = 100% |

### `GetOverallBmiRatio_FourTypes_Each25Percent`

| Given | Then |
|-------|------|
| 4분류 각 1명(연령 25·35·45·55) | type 100~400 각 25% |

---

## 6. 변경 파일

| 파일 | 변경 요약 |
|------|-----------|
| `src/main/cpp/SHealth.h` | `overallBmiRatios_`, `getOverallBmiRatio`, `aggregateOverallRatios` |
| `src/main/cpp/SHealth.cpp` | 전체 집계·조회 구현 |
| `src/test/cpp/SHealthBMITest.cpp` | TC 2건 |
| `README.md` | Activity 4 전체 비율 `[x]` |

---

## 7. 검증

```text
100% tests passed, 0 tests failed out of 29
```

---

## 8. 결론

Activity 4 Phase F5 및 README Activity 4 **5개 체크박스**를 모두 완료했다. `getOverallBmiRatio`로 연령대와 무관한 전체 BMI 분포를 조회할 수 있다.
